---
/* Same vertical marquee at every width. On a phone it moves above the copy — faces first, then
   the pitch — which is a grid `order` change and nothing else: the component lays out exactly
   as it does on desktop. */
import { getCopy } from "../i18n";
import { startUrl } from "../data/site";
import ReelMarquee from "./ReelMarquee.astro";

const { hero } = getCopy(Astro.currentLocale);
---

<section class="hero">
  <div class="wrap hero-grid">
    <div class="hero-copy reveal">
      <div class="eyebrow">{hero.eyebrow}</div>
      <h1 class="disp h1x">{hero.title}</h1>
      <p class="lead">{hero.lead}</p>
      <div class="hero-cta">
        <a class="btn btn-primary btn-lg" href={startUrl(Astro.currentLocale)}><span>{hero.ctaPrimary}</span></a>
        <a class="btn btn-ghost btn-lg" href="#showcase"><span class="play">▶</span><span>{hero.ctaSecondary}</span></a>
      </div>
      <div class="microcopy">{hero.microcopy}</div>
    </div>
    <div class="hero-visual"><ReelMarquee /></div>
  </div>
</section>
