# apps/

Deployable applications. Each is scaffolded in its own build phase:

- `landing/` — Astro marketing site (Phase 5) → Cloudflare Pages
- `web/` — Next.js web app (Phase 4) → Railway
- `api/` — Fastify API (Phase 2) → Railway
- `worker/` — Python video worker (Phase 3) → Railway
